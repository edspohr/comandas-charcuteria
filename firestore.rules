rules_version = '2';

// Placeholder rules for milestone 1 — hardened in milestone 13.
// While emulators are used exclusively, allowing authenticated read/write is safe.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
